apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: litefunctions-operator
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: litefunctions-operator-metrics-service
  namespace: litefunctions
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: litefunctions-operator
    app.kubernetes.io/name: operator
